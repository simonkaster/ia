Introduction
Pendulum, very simple, but very useful. This is a one of most simple machine in the human history, and we use it for the clock calculate because it can have the power which is cheap, exactly, and can mad =e the clock stay right.
But Pendulum hav
